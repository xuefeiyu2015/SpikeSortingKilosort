# Authoring laptop. No CUDA, no CatGT, no TPrime.
#
# Useful for: config validation, the pure-compute test suite, alignment and
# export on already-sorted results, and the demo pipeline on CPU (slow).
data_root: "~/ephys"
output_root: "~/ephys/sorted"
cache_dir: "~/ephys/cache"

catgt_dir: null
tprime_dir: null

device: "cpu"
